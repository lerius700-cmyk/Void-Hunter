# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
*.egg

# Virtual envs
.venv/
venv/
env/
ENV/
.build-venv/   # BLOQUE 58.14.7: uv venv for pyinstaller + pygame rebuild

# Test / coverage
.pytest_cache/
htmlcov/
.coverage
.coverage.*
coverage.xml
.tox/

# Mypy
.mypy_cache/

# Ruff
.ruff_cache/

# Editor
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Logs
*.log
logs/

# Local artifacts
.pygame/
.macosx/
run_output.txt
highscores.json

# Secrets / env
.env
.env.local
secrets/

# PyInstaller build artifacts (rebuild with `pyinstaller build.spec`)
build/
dist/
*.spec.bak

# Legacy content (kept in repo as archive, but excluded from build dist)
archive/

# Release artifacts (generated on demand, not part of source)
# - releases/ is the historical pattern (plural)
# - release/  is the current pattern (singular, used for v1.0.0+)
releases/*.zip
release/
release/*.zip
release/_backup_old_releases/

.superpowers/

# Generated sprite intermediates (1024x1024 source images before
# post-processing into the final 16-bit pixel-art PNGs). The final
# sprites in stellar_horizon/assets/sprites/*.png ARE tracked.
stellar_horizon/assets/sprites/_gen_*.png
