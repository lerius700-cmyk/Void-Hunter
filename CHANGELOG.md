# CHANGELOG

All notable changes to VOID HUNTER are documented here. The format is
based on [Keep a Changelog](https://keepachangelog.com/), and this
project adheres to [Semantic Versioning](https://semver.org/).

---

## [Unreleased] — master

The `master` branch is the bleeding edge. It may contain WIP commits
between tagged releases. For stable downloads, use a tagged release.

Notable in-progress work:
- Pre-existing audio SFX count test (33 vs 26) — fixed (was 26, now 33)
- Pre-existing lowpass WAV test (was failing on missing numpy in
  build venv) — fixed by adding numpy to the build venv

---

## [v1.1.0] — 2026-08-18

> Pixel art nebulae + bomb VFX + 50 choreographed enemy patterns.

### Added

**BLOQUE 58.14.6 — Pixel art nebulae**
- New `Assets/background/galaxy_pixelart_*.png` (4 tinted variants: blue,
  red, cyan, violet) generated by `tools/tint_galaxy_sprites.py` (HSL
  hue rotation, preserves saturation/lightness for chunky pixel art).
- Parallax now uses these pixel art sprites instead of the old AI high-res
  galaxies. NEAREST scaling preserves the chunky aesthetic.
- Gameplay uses 4 nebulas (was 2) with `nebula_radius 60-90`.

**BLOQUE 58.14.7 — Bomb VFX**
- New `src/fx/bomb_burst.py`: 3 concentric soft rings (yellow → orange
  → red-orange), 0.4s life, scale + fade animation.
- Wired in `src/ui/gameplay_runtime.py` (trigger on bomb, update each
  frame, draw after shockwave).

**BLOQUE 58.14.7 — 50 choreographed enemy patterns**
- New `src/systems/wave_patterns/composed.py`: 9 formations × 8 bezier
  paths × 3 follow modes × counts 4-8 = 50 combinations.
- Each `ComposedPattern.generate()` outputs a `WavePatternResult` with
  `extra={segments, segment_durations, formation, path, follow}` in
  the format `runtime.py` reads via `attach_multi_segment_path`.
- New `SoloEnemySpawner`: 1 red solo ship every 5s, with a slight curve
  path. Independent of pattern timing for ambient variety.

**BLOQUE 58.14.7 — Manager wire**
- `WavePatternKind.COMPOSED = "composed"` added to the enum.
- `ProceduralWaveManager._composed_patterns: list[WavePattern]` added.
- `register_composed_patterns()` method: registers all 50 in one call.
- `pick_pattern()` handles kind=COMPOSED by `rng.choice` from the pool
  (graceful fallback to BEZIER_SWEEP if pool is empty).
- COMPOSED added to weighted pool at 20/24/28 per floor (highest
  single weight), so the 50 patterns get meaningful screen time
  (~18% of picks in smoke test).
- `GameplayRuntime.enable_procedural_patterns()` now calls
  `register_composed_patterns()` and instantiates `SoloEnemySpawner`.
- `GameplayRuntime._spawn_procedural_patterns()` ticks the solo
  spawner and spawns the returned ships.
- New `src/systems/wave_patterns/runtime.py::spawn_solo_ship()` helper.

**BLOQUE 58.14.8 follow-up — Variety from your reference**
- 4 pixel art galaxy tints (blue, red, cyan, violet) generated from the
  user's wave intro reference (`galaxy_pixelart_reference.png`).
- `tools/extract_galaxy_sprite.py`: reproducible crop + transparency.
- `tools/tint_galaxy_sprites.py`: reproducible HUE shift generator.

### Changed
- `src/systems/parallax.py`: `_GALAXY_SPRITE_PATHS` now points to 4
  pixel art variants. NEAREST scaling, aspect ratio preserved, no
  circular mask (transparent background handles edges).
- `src/ui/gameplay_runtime.py`: nebula_count 2→4, radii 80-110→60-90.
- `tests/test_bloque_58_10.py`, `tests/test_wave_patterns.py`: updated
  pool count expectations (6→7) and added `mgr.register_composed_patterns()`
  to picking tests so COMPOSED is actually pickable.

### Fixed
- 5 pre-existing test failures (audio SFX count + lowpass WAV) that
  had been silently broken in earlier sessions — now all 1171 tests
  pass.
- The `.build-venv` was missing `numpy`, which the lowpass test needs.
  Fixed by `tools/setup_build_venv.bat`.

### Verified
- Smoke test (`tools/smoke_test_composed_wire.py`): all 6 tests pass.
  500 picks: COMPOSED = 89 (18%), all with valid segments.
- Full pytest: 1171/1171 (was 1166/1171).
- In-game visual: `release/topmost_225135_8.png` shows
  `PATTERN: COMPOSED` in the HUD, all 4 galaxy tints visible, solo
  enemy red ship spawning, bomb VFX (orange ring) on B key.

---

## [v1.0.0] — 2026-08-15

> First public release. Standalone `.exe` (4.63 MB launcher + 300MB
> `_internal/`) for Windows x64.

### Notes
- v1.0.0 was published on GitHub Releases but `master` has moved
  forward since (commits `4adcc0d` boss+rings, `cc804ae` visuals,
  `20983a6` wire, `c2951a4` test fixes, `8dc899d` gitignore,
  `dea6a16` + `f2f72df` pixel art). If you downloaded v1.0.0, the
  .exe does NOT include those features. **v1.1.0** is the next
  release that includes them.

## [v1.1.1] — 2026-08-19

> Patch release: rebuild .exe with `synth.py` fix + ship `docs/movement/`.

### Fixed
- `src/audio/synth.py::apply_lowpass_to_wav()` — the numpy IIR loop is
  now wrapped in `try/except`. Before: if numpy failed, the game would
  raise. After: returns `False` gracefully and the game keeps running.
  The v1.1.0 `.exe` was built **before** this fix landed, so v1.1.1
  re-bundles a `.exe` that includes it.

### Added
- `docs/movement/` — full documentation of the choreography system
  (BezierPath, WaypointPath, HybridPath, FlightFormation, PathFollower,
  6 base + 50 COMPOSED wave patterns, ParallelPathPair, OrbitalPath).
  6 files, 1223 lines. The "soul of the game" finally written down
  so future agents (human or AI) know what NOT to refactor.

### Verified
- Game launches with `VOID_HUNTER_PATTERNS_SEED=42`, fires
  `PATTERN: COMPOSED` correctly, all 4 pixel art nebula tints
  visible, solo red enemy spawning, bomb +1 power-up works.
- 1171/1171 pytest pass.

## [v1.1.2] — 2026-08-20

> Arc-length motion + per-instance jitter + parametrize 50 COMPOSED tests.

### Added

**BezierPath arc-length parameterization (BLOQUE 58.next)**
- 64-sample `(t, distance)` table pre-computed in `__init__` (~0.01% error).
- New API on `BezierPath`: `position_at_distance(s)`, `tangent_at_distance(s)`,
  `total_arc_length`.
- `HybridPath` propagates per-segment arc lengths; new
  `position_at_distance(s)` / `tangent_at_distance(s)` for the whole path.
- `PathFollower` now walks arc length `s` (in pixels) instead of
  parameter `t`. Velocity is constant in screen space — no more
  faster-in-straights / slower-in-curves.

**Per-instance jitter in `ComposedPattern.generate()`**
- Each pick perturbs: control points ±12 px, slot offsets ±10%, colors
  ±25 channels (clamped 0-255), `duration_s` ±10%, `t_offset` ±0.10s.
- Two picks of the same (formation, path, follow) recipe now look
  different every time. Same outer seed stays deterministic.
- Bounded so the shape is still recognizable (a V stays a V, a sweep
  stays a sweep, just different in detail).

**Tests (BLOQUE 58.next)**
- `tests/test_composed_all_50.py`: 50 patterns × 7 assertions = 350 tests.
- `tests/test_jitter_variety.py`: 30 tests for variety + bounds.
- `tests/test_movement.py`: extended with arc-length API tests.
- New `tools/capture_choreo.py` (PIL-based screenshot helper for the
  running game window — replacement for the missing
  `capture_always_on_top.ps1`).

### Changed
- `src/movement/follower.py` — internal state changed from `t` to `s`.
  The `t` property is kept (computed) for backward compat.
- `src/movement/hybrid.py` — `_segment_for_t` got a sibling
  `_segment_for_s`; both work, `_segment_for_s` is used internally now.

### Verified
- 1609/1609 pytest pass (was 1171; +438 new from the 50-COMPOSED
  parametrize and the arc-length / jitter tests).
- In-game: COMPOSED pattern picked with seed 42 produces visibly
  different waves on consecutive picks (verified in `logs/patterns.log`
  — durations jitter around 8.0s as expected).

[Unreleased]: https://github.com/lerius700-cmyk/Void-Hunter/compare/v1.1.2...HEAD
[v1.1.2]: https://github.com/lerius700-cmyk/Void-Hunter/compare/v1.1.1...v1.1.2
[v1.1.1]: https://github.com/lerius700-cmyk/Void-Hunter/compare/v1.1.0...v1.1.1
[v1.1.0]: https://github.com/lerius700-cmyk/Void-Hunter/compare/v1.0.0...v1.1.0
[v1.0.0]: https://github.com/lerius700-cmyk/Void-Hunter/releases/tag/v1.0.0
